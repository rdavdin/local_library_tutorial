extends layout

block content 
    h1= title 
    ul 
        each  book in book_list 
            li 
                a(href=book.url) #{book.title} 
                | (#{book.author.name})
        else 
            li There are no books.
    p Try include a js script 
    script 
      include ../public/javascripts/trycor.js